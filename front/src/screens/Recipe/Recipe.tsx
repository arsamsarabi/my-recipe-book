import React, { FC, ReactElement } from 'react'

interface RecipeProps {}

const Recipe: FC<RecipeProps> = (): ReactElement => {
  return (
    <div>
      <h1>Recipe</h1>
    </div>
  )
}

export default Recipe
